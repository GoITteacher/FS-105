/**
 * Масиви
 *
 * - Створення масиву
 * - Доступ до елементів
 * - Перевизначення значення елемента
 * - Довжина масиву
 * - Індекс останнього елемента
 * - Перевизначення
 */
// =========================

// const arr = [10, 'hello', ];
// console.log(arr);

// =========================
/* const courses = ['HTML', 'CSS', 'JavaScript', 'React', 'PostgreSQL'];

courses[1] = 'Jacob';
courses[3] = 'Adrian';
courses[0] = false;
courses[2] = 5;

console.table(courses); */

/* const numbers = [1, 2, 3, 4, 5, 6, 7];

numbers[0] += 5;
numbers[0]++;
numbers[2]--;

console.log(numbers); */
// ===============================
// const courses = ['HTML', 'CSS', 'JavaScript', 'React', 'PostgreSQL'];

// console.log(courses.length);

// const lastIndex = courses.length - 1;
// console.log(lastIndex);

// console.log(courses[courses.length - 1]);

// ================================

// courses[courses.length - 1] = 'END';

// courses[99] = 'Hello';

// console.log(courses);

// =================================

// const arr = [5, 1, 3, 6, 8, 3];

// arr[0] = arr[5];

// arr[7] = arr[0] + arr[1];

// console.log(arr);

// ================================

// delete arr[1];
// delete arr[3];

// console.log(arr);
